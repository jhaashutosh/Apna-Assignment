import FeaturedComponent from '../components/FeaturedComponent';

export function Home() {
    return (
        <div style={{marginTop: '450px'}} className="w-full h-screen flex justify-center items-center">
            <FeaturedComponent />
        </div>
    );
}